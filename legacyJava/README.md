BootsFaces - Legacy Java versions Builds
========================================

java6 : NOT Supported anymore
java7 : you can still build it with the following gradlew command:
./gradlew :legacyJava:java7:defaultJar

